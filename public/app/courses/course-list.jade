.container(data-ng-controller="mvCourseListCtrl as vm")
    table.table.table-hover.table-striped.table-condensed
        thead
            tr
                th Title
                th Publish Date
        tbody
            tr(ng-repeat="course in vm.courses")
                td {{course.title}}
                td {{course.published | date}}

